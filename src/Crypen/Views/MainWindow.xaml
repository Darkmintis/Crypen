<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Crypen.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Crypen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="Crypen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation View -->
        <NavigationView x:Name="NavView" 
                      Grid.Row="0" Grid.RowSpan="2"
                      PaneDisplayMode="Left"
                      IsSettingsVisible="True"
                      SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard" Tag="dashboard" Icon="Home"/>
                <NavigationViewItem Content="Encrypt" Tag="encrypt" Icon="Lock"/>
                <NavigationViewItem Content="History" Tag="history" Icon="Clock"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Padding="20"/>
        </NavigationView>
    </Grid>
</Window>
