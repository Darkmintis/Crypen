<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Crypen.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Crypen"
    xmlns:converters="using:Crypen.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converters -->
            <converters:StatusToVisibilityConverter x:Key="StatusToVisibilityConverter"/>
            <converters:EmptyCollectionToVisibilityConverter x:Key="EmptyCollectionToVisibilityConverter"/>
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:FileSizeConverter x:Key="FileSizeConverter"/>
            <converters:DateTimeConverter x:Key="DateTimeConverter"/>

            <!-- Colors -->
            <SolidColorBrush x:Key="WindowCaptionBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="WindowCaptionBackgroundDisabled" Color="#2D2D2D"/>
            <SolidColorBrush x:Key="WindowCaptionForeground" Color="White"/>
            <SolidColorBrush x:Key="WindowCaptionForegroundDisabled" Color="Gray"/>
            
            <!-- Main color scheme -->
            <Color x:Key="CrypenPrimaryColor">#118AB2</Color>
            <Color x:Key="CrypenSecondaryColor">#073B4C</Color>
            <Color x:Key="CrypenAccentColor">#06D6A0</Color>
            <Color x:Key="CrypenWarningColor">#FFD166</Color>
            <Color x:Key="CrypenErrorColor">#EF476F</Color>
            
            <SolidColorBrush x:Key="CrypenPrimaryBrush" Color="{StaticResource CrypenPrimaryColor}"/>
            <SolidColorBrush x:Key="CrypenSecondaryBrush" Color="{StaticResource CrypenSecondaryColor}"/>
            <SolidColorBrush x:Key="CrypenAccentBrush" Color="{StaticResource CrypenAccentColor}"/>
            <SolidColorBrush x:Key="CrypenWarningBrush" Color="{StaticResource CrypenWarningColor}"/>
            <SolidColorBrush x:Key="CrypenErrorBrush" Color="{StaticResource CrypenErrorColor}"/>
            
            <!-- Button Styles -->
            <Style x:Key="CrypenPrimaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource CrypenPrimaryBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
            
            <Style x:Key="CrypenSecondaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource CrypenSecondaryBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
            
            <Style x:Key="CrypenAccentButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource CrypenAccentBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
            
            <Style x:Key="CrypenWarningButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource CrypenWarningBrush}" />
                <Setter Property="Foreground" Value="Black" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
            
            <Style x:Key="CrypenErrorButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource CrypenErrorBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
</Application>
